<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.fyl.springboot.database.mapper.UserMapper">

    <!-- 查询用户信息 -->
    <select id="findByUsername" parameterType="String" resultType="top.fyl.springboot.database.entity.User">
        SELECT *
        FROM users
        WHERE username = #{username} AND delete_flag = 0
    </select>

    <!-- 插入新用户 -->
    <insert id="insertUser" parameterType="top.fyl.springboot.database.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, create_time, update_time, delete_flag)
        VALUES (#{username}, #{password}, #{email}, NOW(), NOW(), 0)
    </insert>

    <select id="findById" parameterType="int" resultType="top.fyl.springboot.database.entity.User">
        SELECT id, username, email, create_time, update_time
        FROM users
        WHERE id = #{id} AND delete_flag = 0
    </select>
</mapper>